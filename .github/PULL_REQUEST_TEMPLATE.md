## Summary

What this PR changes, in 1-3 bullets.

## Motivation

Why we want this. Link the issue if there is one.

## What changed

- Files touched
- New endpoints / commands / settings (if any)
- Schema changes to `/api/run-report` or any other API surface

## How it was tested

- [ ] Compiles cleanly (`arduino-cli compile`)
- [ ] Flashed to a XIAO ESP32-C3 and the boot banner is clean
- [ ] Frontend hard-refreshed and the affected screen renders
- [ ] If touching the run pipeline: a real Start → walk → Stop produced sensible numbers

## Open follow-ups

Anything intentionally left for a future PR.
